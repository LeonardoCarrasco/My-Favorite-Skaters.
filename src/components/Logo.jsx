import { Link } from "react-router-dom"

const Logo = () => {
    return (
        <Link to='/' href="#" className='logo hover:transition-all hover:scale-105'>
        MFS.
      </Link>
    )
}

export default Logo